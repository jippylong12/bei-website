User-agent: *
Allow: /

# BitResearch is shared by direct link only. It loads for anyone holding the
# URL, but it is not linked from btcedu.org and must stay out of search results.
Disallow: /BitResearch
Disallow: /research

# The SwIRL handbook bot is for staff. This path only forwards to it; the bot
# itself is hosted separately and never served from this site. Keeping it out of
# search matters more now that the bot itself has no login.
Disallow: /handbot
